[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pantry-pulse"
version = "0.1.0"
description = "Demand forecasting and decision support for community food pantries."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
